{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "启动 Chrome",
      "url": "http://localhost:3000/zyzl-admin/?MCH_MANAGEMENT",
      "webRoot": "${workspaceRoot}/public/sites/"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "启动 Server",
      "program": "${workspaceRoot}/bin/www",
      "args": [
        "--color"
      ],
      "cwd": "${workspaceRoot}"
    }
  ]
}